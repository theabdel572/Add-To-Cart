{
  "name": "add-to-cart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "doppler run -p add-to-cart -c dev --watch -- vite",
    "build": "doppler run -p add-to-cart -c prd --watch -- vite build",
    "preview": "doppler run -p add-to-cart -c prd --watch -- vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.10"
  }
}