{
  "name": "add-to-cart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "doppler run -p add-to-cart --watch -- vite",
    "build": "doppler run -p add-to-cart --watch -- vite build",
    "preview": "doppler run -p add-to-cart --watch -- vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  }
}