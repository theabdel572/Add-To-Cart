{
  "name": "add-to-cart",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "doppler run --watch -- vite",
    "build": "doppler run --watch -- vite build",
    "preview": "doppler run --watch -- vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5"
  }
}